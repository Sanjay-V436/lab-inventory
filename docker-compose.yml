services:

  db:
    image: postgres:16-alpine
    container_name: lab_db
    restart: always
    environment:
      POSTGRES_DB: lab_inventory
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: sanjay2006
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./server/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"

  server:
    build: ./server
    container_name: lab_server
    restart: always
    depends_on:
      - db
    environment:
      PORT: 5000
      DATABASE_URL: postgresql://postgres:sanjay2006@db:5432/lab_inventory
      JWT_SECRET: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwidXNlcm5hbWUiOiJsYWJhZG1pbiIsImlhdCI6MTc3MzUwNTYzMCwiZXhwIjoxNzczNTM0NDMwfQ.VGvEhB3KHu9iRiFiZZWeQPzNVdATIIQtgmaUpaH3lBM
      CLIENT_URL: http://localhost
      CLOUDINARY_CLOUD_NAME: dtuueykhx
      CLOUDINARY_API_KEY: 377183914961547
      CLOUDINARY_API_SECRET: ZHOLaNhqvTz6vCxQKaSH8VhCjFs
      EMAIL_USER: rhisclab@gmail.com
      EMAIL_PASS: wwyh wnow bchc gbmz
    volumes:
      - uploads_data:/app/uploads
    ports:
      - "5000:5000"

  client:
    build: ./client
    container_name: lab_client
    restart: always
    depends_on:
      - server
    ports:
      - "80:80"

volumes:
  postgres_data:
  uploads_data: